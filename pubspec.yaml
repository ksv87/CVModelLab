name: cv_model_lab
description: Core evaluation logic for CV Model Lab.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.7
  archive: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

